# Settings to manage and configure a Firebase App Hosting backend.
# https://firebase.google.com/docs/app-hosting/configure

runConfig:
  # The number of server instances that can run for your app.
  # Increase this value to automatically spin up more instances in response
  # to increased traffic for better performance and availability.
  # Consider the cost implications of running more instances.
  maxInstances: 1
